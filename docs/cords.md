# コードパッケージ

Cords パッケージ (「コード ライブラリ」とも呼ばれます) は、次の文字列パッケージです。
ツリーベースの表現。

提供される基本関数の説明については、`cord.h`ファイルを参照してください。  そして、
`ec.h`ファイルには「拡張可能なコード」が記述されており、これらは本質的に出力ストリームです
コードに書き込むもの。これらにより、コードの効率的な構築が可能になります。
コードのサイズに制限が必要です。

`cord`ライブラリは、手動で実行しない限り、デフォルトで`gc`ライブラリとともにビルドされます。
無効 (例: cmake ベースのビルドの場合、`-D build_cord=OFF`オプションを除く)
`cmake`に渡されます)。

データ構造の詳細については、Boehm、Atkinson、および Plass を参照してください。
[「ロープ: 文字列の代替案」](https://ondoc.logand.com/d/686/pdf)、
ソフトウェアの実践と経験、25、12、1995 年 12 月、1315 ～ 1330 ページ。

基本的に同様の「ロープ」データ構造も SGI の標準の一部です
テンプレート ライブラリの実装とその子孫。
GNU C++ ライブラリ。  これはデフォルトで参照カウントを使用します。  短いものがあります
そのデータ構造の説明
[Rope 実装の概要](https://www.boostcpp.org/sgi/stl/ropeimpl.html)。

これらはすべて、Xerox Cedar の「ロープ」の子孫です。

`cord/tests/de.c`ファイルは、次の使用方法を説明する非常に愚かなテキスト エディタです。
コード。  ファイルのバージョンのリストを維持します。  各バージョンは単なるコードです
ファイルの内容を表します。  ただし、標準的な編集操作は次のとおりです。
非常に大きなファイルでも効率的です。  （その 3 行の「ユーザーマニュアル」は、
引数なしで呼び出すことで取得されます。`^R^N`と`^R^P`が移動することに注意してください。
カーソルはほぼ 1 画面分あります。  タブを理解できないため、次のように表示されます。
強調表示された「I」として表示されます。  最初に UNIX`expand`プログラムを使用します。)
エディターで、bdwgc ルート ディレクトリに`make de`と入力します。

`CORD_printf`とその友人たちは変数番号を含む C 関数を使用することに注意してください
標準に準拠していない方法での引数。  このコードは壊れることが知られています
一部のプラットフォーム、特に PowerPC。  残りも構築できるはずです
任意のプラットフォーム上のライブラリ (`cordprnt.c`ファイルを除くすべて)
コレクターをサポートします。
