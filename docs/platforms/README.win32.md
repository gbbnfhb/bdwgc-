コレクターはさまざまな時点で Windows 95 以降でコンパイルされてきましたが、
NT、および XP、オリジナルの Microsoft SDK、Visual C++ 2.0、4.0、および 6、
GNU Win32 ツール、Borland C++ Builder、Watcom C、EMX、
Digital Mars コンパイラー (DMC) を使用します。

歴史的な理由から、
コレクタテストプログラム「gctest」はGUIアプリケーションとしてリンクされており、
しかし、窓は開きません。  通常、その出力はファイルに表示されます
「gctest.gc.log」。  ファイルマネージャーから起動することも可能です。  砂時計
カーソルは実行中であれば表示される場合があります。  からスタートした場合は、
コマンドラインの場合、通常はバックグラウンドで実行されます。  少し待ってください
出力を確認するまでに数分 (最新のマシンでは数秒) かかります。
障害を示すメッセージ、または「コレクターは動作しているようです」というメッセージが表示されるはずです。
メッセージ。

コード (ヘビー級) に基づくおもちゃエディター (de.exe)
ツリーとして表される文字列) が移植され、含まれています。
Win32 または win32s のどちらでも正常に動作します。  それは例として役立ちます
本物の Windows アプリケーションですが、それが作成されたものであるという点が異なります。
非専門家 Windows プログラマー。  (多少の特徴はありますが
ファイルの表示方法。  <cr> が明示的に表示されます
標準の DOS テキスト ファイル用。  UNIX 版と同様に、制御
文字は明示的に表示されますが、この場合は赤いテキストとして表示されます。
これは、好みやデフォルト設定によっては最適ではない可能性があります。
窓の色。）

一般に、「-D REDIRECT_MALLOC」は、
アプリケーションは完全に静的にリンクされています。

コレクターは通常、VirtualAlloc を使用して OS からメモリを割り当てます。
これにより、Windows NT および Windows 2000 では問題が発生するようです (ただし、
Windows 95/98 ではありません) メモリが後で CreateDIBitmap に渡される場合。
この問題を回避するには、「-D USE_GLOBAL_ALLOC」を使用してコレクターをビルドします。

[スレッドと増分コレクションについては、以下の最後の方で説明します。]

マイクロソフトツール
---------------

Microsoft 開発ツールの場合は、次のように入力します。
「nmake -f NT_MAKEFILE cpu=i386 disable_threads=1 Enable_static=1 nodebug=1」
コレクターのリリースバリアントを静的ライブラリとしてビルドするには、
マルチスレッドのサポート。

C++ インターフェイスを使用するには、クライアント コードに gc_cpp.h を含める必要があります。
および/または gc_allocator.h。

[gctest については上記を参照してください。]

クライアントは、gc.h をインクルードする前に GC_NOT_DLL を定義する必要がある場合があります。
コレクターは静的ライブラリとして構築されました。

GNU ツール
---------

コレクターは、Cygwin でビルド可能である必要があります。
「./configure; make check」機構。

MinGW ビルド (x64 用を含む) は、直接 (Windows 上でも) 利用できます。
ホスト）およびクロスコンパイル経由（例:
"./configure --host=i686-pc-mingw32; チェックを行う"

デフォルトでは、configure は make にコレクタを DLL (共有) として構築するように指示します。
ライブラリ)、CFLAGS に「-D GC_DLL」を追加します。

平行マーカーはデフォルトで有効になっています。それは次によって無効にされる可能性があります
「--disable-Parallel-mark」オプション。

メモリのアンマッピングは、「--disable-munmap」オプションでオフにすることができます。

ボーランドツール
-------------

Borland ツール (C++Builder など) の場合は、`cmake -G "Borland Makefiles"`を使用します。

Borland のコンパイラは、デフォルトで構造内で 1 バイトのアラインメントを行うことに注意してください。
(-a1)、一方、Visual C++ はデフォルトで 8 バイト アライメントになっているようです。
デフォルト構成のガベージ コレクターは少なくとも 4 バイトを期待します。
アライメント。  したがって、Borland のデフォルトをオーバーライドする必要があります。  （私の意見では、
とにかく通常はそうあるべきです。  -a1 によって大きなパフォーマンスがもたらされることを期待しています
486 または Pentium ではペナルティが発生します。) これにより構造レイアウトが変更されることに注意してください。
(最後の手段として、「-D FORCE_ALIGNMENT_ONE」オプションを
コンパイラは 1 バイトのアラインメントを許可しますが、これは重大なマイナスになります。
パフォーマンスへの影響。)

Digital Mars コンパイラ
---------------------

MS Visual C++ と同じですが、必要な場合があります
「-D AO_OLD_STYLE_INTERLOCKED_COMPARE_EXCHANGE」オプションを使用してコンパイルします。
平行マーカーが有効になりました。

コレクターを構築するには、次のコマンドを使用します。
make -f digimars.mak

いつものように、テストを実行する`check`目標を指定します。
-f digimars.mak チェックを行う

ワトコムコンパイラ
---------------

Watcom ポートに関する Ivan V. Demakov の README:

コレクターは Watcom C 10.6、11.0、および OpenWatcom 2.0 でテストされています。
Win32 および win32s で動作し、さらに dos4gw を使用する DOS でも動作します。
dos エクステンダー。  Win32 では、コレクタは dll として構築できます。
または静的ライブラリとして。

すべてのコンパイルは Windows 95、NT、Windows 2K または Windows 2K で行われたことに注意してください。
後で。  理由は不明ですが、Windows 3.11 for NT でコンパイルしています (1 つ)
試行が行われた場合)、実行可能ファイルが破損する可能性があります。

増分収集がサポートされています (MSDOS を除く)。

コレクターを構築するには、次のコマンドを使用します。
wmake -f WCC_MAKEFILE

ビルドのカスタマイズ、つまりターゲット プラットフォームやライブラリの設定が必要になる場合があります。
タイプ (動的または静的)、呼び出し規約、および最適化オプション。
この場合、追加の引数を wmake に渡します。例:
- 「ENABLE_STATIC=1」: コレクターを静的ライブラリとしてビルドします。
- 「SYSTEM=DOS4GW」は dos4gw 実行環境を対象とします。

テストを実行するには、`check`目標を指定します。
wmake -f WCC_MAKEFILE チェック

libgc を使用するすべてのプログラムは、4 バイト アラインメントでコンパイルする必要があります。
これに関する詳しい説明については、Borland コンパイラに関するコメントを参照してください。

libgc が dll としてコンパイルされる場合は、マクロ「GC_DLL」を事前に定義する必要があります。
「gc.h」を含む (たとえば、「-D GC_DLL」コンパイラ オプションを使用)。それは
重要です。そうでないと、結果として得られるプログラムは実行されません。

コレクターをコンパイルする別の方法は、cmake ビルド システムを使用することです。
cmake -G "Watcom WMake" 。
cmake --build 。

OpenWatcom ユーザーへの特別な注意事項: (C++ とは異なる) C コンパイラー (
最新の安定リリース、古いリリースについては不明）ポインタをグローバルに強制しません
変数 (つまり、構造体フィールドではない、ローカルでは不明) は、次の場合を除いて整列されます。
速度の最適化 (例: "-ot" オプションが設定されている); 「-zp」オプション（または整列）
pragma) は構造体のアライメントのみを制御します。バグかどうかはわかりません
または機能ですが、警告が表示されます。

増分コレクション
----------------------

増分コレクションはある程度サポートされています。  デフォルトでは、
コレクターは明示的なページ保護と GetWriteWatch ベースのどちらかを選択します
プラットフォームに応じて、追跡を自動的に書き込みます。

前者は速度が遅く、デバッガーとの対話性が不十分です。
ページは保護されています。  保護違反はハンドラーによって捕捉されます
ハンドラーの下部に取り付けられています
スタック。  可能な限り、次の宛先への通話を追加することをお勧めします。
GC_enable_incremental は、ほとんどの時間が経過した後、可能な限り最後の瞬間に
デバッグが完了しました。  システムなし
呼び出しはコレクター自体によってラップされます。  必要かもしれない
ヒープ内のバッファを使用する ReadFile 呼び出しをラップするため、
呼び出しの実行中に保護違反が発生することはありません。
(いつものように、GC_enable_incremental でない限り、これは問題になりません。
と呼ばれます。）

「-D SMALL_CONFIG」を指定すると増分収集が無効になることに注意してください。

スレッド
-------

デフォルトでは、コレクターは他のプラットフォームと同様にスレッドを処理します。
コレクター DLL に接続されたスレッドを追跡する James Clark のコード
存在しますが、両方が必要です
- コレクターは GC_DLL が定義された DLL に組み込まれており、
- GC_use_threads_discovery() はコレクターの初期化の前に呼び出されます。
これは、追加のスレッドを作成する前に実行する必要があります。
一般的には、可能であればこれを避けることをお勧めします。
信頼性が 100% 未満である可能性があります。

スレッドを同様に処理する動的ライブラリとしてコレクタを構築するには
他のプラットフォームの場合は、「nmake -f NT_MAKEFILE」と入力します。

増分コレクションは、追加のコレクションの前に有効になっている場合にのみサポートされます。
追加のスレッドが作成されます。

スレッドは、Microsoft ツールを使用した静的ライブラリのビルドでもサポートされています
(例: NT_MAKEFILE)、および CMake および GNU ツールを使用します。  コレクター
GC_THREADS を定義してビルドする必要があります (これは NT_MAKEFILE のデフォルトです)。
CMakeLists.txt、build.zig、configure)。

通常の非 DLL ベースのスレッド追跡が適切に機能するには、
スレッドは GC_CreateThread または GC_beginthreadex を使用して作成する必要があります。
通常どおり終了するか、GC_endthreadex または GC_ExitThread を呼び出します。  (Cygwin の場合、
代わりに、標準の pthread_create/exit 呼び出しを使用できます。) pthread と同様
gc.h を含む場合、CreateThread、_beginthreadex、
_endthreadex と ExitThread を使用して、代わりに GC_ バージョンを呼び出します。

いつものように、GC_CreateThread はリソース リークを引き起こす傾向があることに注意してください (
C ランタイム) は、GC_beginthreadex によって回避されます。  現在はありません
_beginthread と同等なので、使用しないでください。

GC_INIT() は、他の呼び出しを行う前にメインスレッドから呼び出す必要があります。
コレクタ。

TerminateThread() Windows API 呼び出しを使用しないことを強くお勧めします。
特にガベージコレクターの場合。  どのような使用も、次のような問題を引き起こす可能性があります。
コレクターがクラッシュするため、コレクターは次のことを行うことができなくなります。
スレッドを正しく追跡します。

MinGW pthreads-win32 (または他の非 Cygwin pthread) のコレクターを構築するには
Windows の実装)、Makefile.direct を使用し、明示的に設定します
GC_WIN32_PTHREADS (または --enable-threads=pthreads を渡して構成)。
静的スレッド ライブラリには「-D PTW32_STATIC_LIB」を使用します。

ライブラリを構築する別の (より良い) 方法は、CMake スクリプトを使用することです。
詳細については、cmake.md ファイルを参照してください。
