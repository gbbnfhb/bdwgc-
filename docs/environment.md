# コレクターの動作に影響を与える環境変数

ガベージ コレクターは、次のような多くの環境変数を調べます。
その後、その動作に影響を与えるために使用されます。

`GC_INITIAL_HEAP_SIZE=<bytes>`- 初期ヒープ サイズをバイト単位で設定します。  メイスピード
アッププロセスの起動。  オプションで、乗数 (「k」、「M」) で指定できます。
または「G」）の接尾辞。

`GC_MAXIMUM_HEAP_SIZE=<bytes>`- 最大ヒープ サイズをバイト単位で設定します。
オプションで、乗数接尾辞を使用して指定できます。

`GC_LOOP_ON_ABORT`- コレクター中止ルーチンがタイトなループに入ります。
これにより、特にそのようなプロセスのデバッグが容易になる可能性があります。
使用可能なコア ファイルを生成しないマルチスレッド プラットフォーム、またはコア
ファイルが大きすぎる可能性があります。  一部のプラットフォームでは、これにより`SIGSEGV`も発生します。
キャッチされ、ハンドラー内で無限ループが発生し、同様のデバッグが可能になります。
テクニック。

`GC_PROMPT_DISABLED`- クライアント コードは非対話モードで実行されています。
設定されている場合、非対話型アクション (エラー時に GUI ダイアログ ボックスを表示するなど)
コレクターでは回避されます。  変数は、
コレクターはテストの実行前にスクリプトを構築します。

`GC_PRINT_STATS`- コレクターのログ記録をオンにします。  の場合は効果がありません。
コレクターは`SMALL_CONFIG`マクロが定義されて構築されています。

`GC_LOG_FILE`- コレクター ログ ファイルの名前を指定します。  それ以外の場合は、
デフォルトでは、ログは`stderr`に実行されます。  コレクターが次の場合は効果がありません。
`SMALL_CONFIG`マクロが定義されてビルドされました。

`GC_ONLY_LOG_TO_FILE`-`GC_stdout`および`GC_stderr`のリダイレクトをオフにします
`GC_LOG_FILE`環境変数で指定されたログ ファイルに保存されます。  ありません
後者が設定されていない限り有効です。コレクターが次のように構築されている場合は効果がありません。
`SMALL_CONFIG`マクロが定義されました。

`GC_PRINT_VERBOSE_STATS`- さらに多くのログ記録を有効にします。  の場合は効果がありません。
コレクターは`SMALL_CONFIG`マクロが定義されて構築されています。

`GC_DUMP_REGULARLY`- コレクタにデバッグ ダンプを生成するように指示します (
`GC_dump_named`) コレクターの初期化時およびすべてのコレクション中に。
非常に冗長です。  報告するバグがある場合に役立ちますが、次の内容のみを含めてください。
最後の完全なダンプ。

`GC_COLLECT_AT_MALLOC=<n>`- で指定されたデフォルト値をオーバーライドします。
`GC_COLLECT_AT_MALLOC`マクロ。  コレクターが構築されない限り効果はありません
後者のマクロが定義されています。

`GC_BACKTRACES=<n>`- コレクターに`n`ランダムを生成するように指示します
各コレクションの後にバックトレース (ヒープ プロファイリング用)。  これはそうではありません
ヒープ内のほとんどのオブジェクトが次の方法で割り当てられていない限り、有用な出力を生成します。
デバッグアロケータ。  これは統計サンプルのみを目的としています。
ヒープの突然変異が同時に発生するため、個々のトレースが間違っている可能性があります。  ありません
コレクタが`KEEP_BACK_PTRS`マクロを定義して構築されていない限り、効果はありません。

`GC_PRINT_ADDRESS_MAP`(Linux のみ) - コレクターにダンプを指示します。
`/proc/self/maps`(つまり、プロセスのさまざまなアドレス マップ) から`stderr`へ
すべてのコレクション。  解読のためのソースにルートアドレスをマッピングするのに役立ちます
リークレポート。

`GC_NPROCS=<n>`(Linux のみ) - プロセッサ コアの数を明示的に設定します。
コレクターが使用することを期待する必要があります。  ただし、これを「1」に設定すると、
複数の CPU コアが使用可能であり、正確性は維持されますが、次のような問題が発生する可能性があります。
アロケータ ロックの実装により、非常にひどいパフォーマンスが発生します。
最初に回転せずにすぐに降伏します。  シングルスレッドには効果がありません
コレクタービルド。

`GC_MARKERS=<n>`- マーカー スレッドの数を設定します。  通常、これは次のように設定されます
プロセッサコアの数。`GC_MARKERS`を 1 つ調整する方が安全です。
`GC_NPROCS`、以前の環境変数は影響を与えないため、
アロケータ ロックの実装。  コレクターが構築されない限り効果はありません
`PARALLEL_MARK`マクロが定義されています。

`GC_LARGE_ALLOC_WARN_INTERVAL=<n>`- コレクターに次の間隔で出力するよう指示します。
非常に大きなブロック割り当てに関する n 番目の警告 (n 番目から始まります)。
`n`の小さな値は、そのような値が制限された数に達するという点で、通常は問題がありません。
警告は通常、最大でも限界のあるリークを示します。  最良の結果を得るには、
テスト中は 1 に設定する必要があります。  デフォルトは 5 です。非常に大きな数値です
警告を効果的に無効にします。

`GC_IGNORE_GCJ_INFO`- コレクターに型記述子を強制的に無視させます
`GC_gcj_malloc()`とその友人によって暗黙的に提供されます。  これは次のような場合に便利です
記述子生成の問題のデバッグ、およびおそらく一時的な作業用
そういう人の周りに。  すべてのヒープ オブジェクトの完全に保守的なスキャンを強制します。
ただし、ポインタが不要であることが知られているものは除き、そのため他の悪影響を及ぼす可能性があります。
効果。

`GC_PRINT_BACK_HEIGHT`- チェーンの最大長の印刷をオンにします。
到達不可能なオブジェクトが到達可能なオブジェクトで終わる。  この数字が残っていたら
境界がある場合、プログラムは「GC 堅牢」になります。  これにより、固定数が確保されます。
誤って識別されたポインターの場合、限定されたスペース リークが発生するだけです。  これ
現在、デバッグ割り当てが全体的に使用されている場合にのみ機能します。  増えます
コレクタのスペースと時間の要件がかなり大きくなります。  この機能はまだあります
やや実験的であり、コレクタが次のように構築されている必要があります。
`MAKE_BACK_GRAPH`マクロが定義されました。

`GC_RETRY_SIGNALS`(pthreads のみ) - 失われたスレッドを補おうとします
一時停止信号と再起動信号。  Tru64 UNIX の場合、またはライブラリの場合はデフォルトでオンになります。
サニタイズされている場合はオフ、それ以外の場合はオフです。  以前にも同様の問題が発生しているため、
他のオペレーティング システムでは、これを有効にするためのランタイム フラグに変わりました。
最後の段階での回避策。  「0」値は「シグナルを再試行しない」ことを意味します。

`GC_USE_GETWRITEWATCH=<n>`(Win32 および Linux のみ) - 明示的に指定します。
ダーティなページを追跡する戦略を使用する必要があります。`n`が「0」の場合、
その後、ページの保護とメモリ障害の捕捉戦略に戻ります)、そうでない場合
コレクターは GetWriteWatch ベースの戦略 (`GWW_VDB`) または Linux を使用しようとします
`userfaultfd`戦略 (`UFFDWP_VDB`)、またはソフト ダーティ ビット戦略
利用可能な場合は (`SOFT_VDB`) を最初に指定します。  コレクターが構築されない限り効果はありません
`MPROTECT_VDB`マクロが定義されており、`GWW_VDB`または`SOFT_VDB`のいずれか、または
`UFFDWP_VDB`マクロが定義されています。

`GC_DISABLE_INCREMENTAL`- インクリメンタルを有効にするランタイム要求を無視します。
ガベージコレクションモード。  デバッグに役立ちます。


## プログラムで設定可能な実行時フラグ用のもの

次のフラグは、プログラムでも設定可能な実行時フラグをオンにします。
コレクタの初期化中にのみチェックされます。  私たちは彼らがそうすることを期待しています
通常は他の方法で設定されますが、これはデバッグやデバッグに役立つ場合があります。
テスト中。

`GC_ENABLE_INCREMENTAL`- 起動時に増分収集をオンにします。  注記
プラットフォームとコレクターの構成によっては、これには次のような問題が含まれる可能性があります。
変更を追跡するためにヒープの一部を書き込み保護します。  これらの作品は、
ポインターのないオブジェクトを含むかどうか。  透明にするつもりですが、
`mprotect`ベースの実装の場合、意図しないシステムが発生する可能性があります。
呼び出しは失敗します (したがって、注意して使用してください)。

`GC_PAUSE_TIME_TARGET`- 必要なガベージ コレクターの一時停止時間を設定します。
ミリ秒 (ms)。  増分コレクションが実行されない限り、効果はありません。
有効になりました。  コレクションにこれよりもかなり長い時間が必要な場合、クライアントは
が再起動され、コレクターは追加の作業を行う必要があります。
補償する。  特別な値「999999」は、一時停止時間が
無制限であり、増分コレクターは完全に単純なコレクターのように動作します。
世代のコレクター。  指定された特別な値を除くすべての値は無効になります。
今のところ平行マーカー（ほぼ完全）。

`GC_FULL_FREQUENCY`- 間の部分コレクションの必要な数を設定します。
完全なコレクション。`GC_incremental`が設定されている場合にのみ問題となります。  以下の場合は効果がありません
コレクターは`SMALL_CONFIG`マクロが定義されて構築されています。

`GC_FREE_SPACE_DIVISOR`-`GC_free_space_divisor`を指定された値に設定します。
より大きな値に設定すると、スペース消費量が減少し、
ガベージコレクションの頻度。

`GC_UNMAP_THRESHOLD`- メモリ ブロックのマッピング解除の必要なしきい値を設定します
(候補ブロックの実行中の連続ガベージ コレクションの数
マッピング解除の場合は無料としてマークする必要があります)。  特別な値「0」を完全に
アンマッピングを無効にします。

`GC_FORCE_UNMAP_ON_GCOLLECT`- 「明示的な GC で可能な限りマップを解除する」をオンにします。
モードがオンになります (デフォルト値をオーバーライドします)。  暗黙的に開始されたものには影響しません
ゴミ収集。  メモリアンマッピングが無効になっている場合（または無効になっていない場合）は効果がありません。
でコンパイルされる)、またはマッピング解除のしきい値が 1 の場合。

`GC_FIND_LEAK`- GC_find_leak をオンにし、リーク検出をオンにします。  強制的に
プログラム終了時に収集して、それ以外の場合に発生するリークを検出します。
最後のコレクションの後。  コレクターが次のように構築されている場合は効果がありません。
`NO_FIND_LEAK`マクロが定義されました。

`GC_FINDLEAK_DELAY_FREE`- オブジェクトの遅延解放をオンにします。
find-leak モード (詳細については、対応するマクロの説明を参照してください)。
コレクターが`NO_FIND_LEAK`マクロを定義してビルドされている場合は、効果はありません。

`GC_ABORT_ON_LEAK`- リークが発生した場合、または
壊れた物が発見される。

`GC_ALL_INTERIOR_POINTERS`- 内部ポインターの認識をオンにします。

`GC_DONT_GC`- ガベージ コレクションをオフにします。  慎重に使用してください。

`GC_USE_ENTIRE_HEAP`- デフォルトの動作をオーバーライドします。
収集前にヒープ全体を使用する非増分コレクター。  を参照してください。
[macros.md](macros.md) の同様のマクロの説明。

`GC_TRACE=<addr>`- デバッガーにトレースに関する情報をログに記録させます。
`addr`を含むアドレス範囲の数。  コレクタのデバッグを目的としています。
通常、`addr`は、次のオブジェクトへのポインタを含むアドレスです。
不思議なことにマークを取得できませんでした。`addr`の値は次のように指定する必要があります。
16 進整数。  コレクターがビルドされていない限り効果はありません。
`ENABLE_TRACE`マクロが定義されました。
