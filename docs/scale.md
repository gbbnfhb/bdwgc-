# ガベージ コレクターのスケーラビリティ

Makefile.direct が使用されている場合、デフォルト設定では
Boehm-Demers-Weiser ガベージ コレクターはスレッドセーフではありません。一般に、それは可能です
`-D GC_THREADS`コンパイルを使用してコレクターをビルドすることでスレッドセーフになりました
フラグ。これには主に次のような影響があります。

1. 必要に応じて、ガベージ コレクターが他のすべてのスレッドを停止します。
一貫したメモリ状態を確認します。スレッドの作成を妨害し、
終了イベントは、次の場合に停止されるクライアント スレッドのリストを維持します。
必要です。

2. 基本的にコレクタがアロケータ ロックを取得するようになります。
すべての割り当てとガベージ コレクション アクティビティ。  このロックは次の用途に使用されるため、
すべての割り当て関連のアクティビティ。割り当てできるスレッドは 1 つだけです
または、ある時点で収集します。これにより本質的にパフォーマンスが制限されます
マルチプロセッサ上のマルチスレッド アプリケーション。

ほとんどのプラットフォームでは、アロケータ ロックはスピン ロックとして実装されます。
指数関数的バックオフあり。より長い待ち時間は、yield によって実装されます。
および/または寝ています。コレクションが進行中の場合、純粋な紡績段階
スキップされます。これには、ほとんどのユニプロセッサがロックするという議論の余地のない利点があります。
買収額は非常に安い。アプリケーションには次のような欠点があります。
やるべき仕事があるときでも、短時間睡眠をとります。そして
スレッドが短期間不必要にウェイクアップされる可能性があります。にもかかわらず、これは、
このスキームは経験的にネイティブのキューベースの相互排他よりも優れたパフォーマンスを発揮します
ほとんどの場合は実装されますが、場合によっては大幅に実装されます。

## スケーラビリティを強化するためのオプション

コレクターは 2 つの機能を使用して、コレクターのスケーラビリティを強化します。
マルチプロセッサ。これらは一緒に使用することを目的としています。 (以下参照
`Makefile.direct`ファイルに再度コピーします。)

*`-D PARALLEL_MARK`を使用してコレクタをビルドすると、コレクタを実行できるようになります
マークフェーズは複数のスレッドで並行して行われるため、複数のスレッドで実行されます。
プロセッサ (またはプロセッサ コア)。マークフェーズは通常、大量のデータを消費します。
収集時間の大部分。したがって、これによりガベージが大幅に並列化されます。
割り当てプロセスではありませんが、コレクター自体。現在のマーキングは
コレクションをトリガーしたスレッドによって実行されます。
`n - 1`専用スレッド。`n`はプロセッサ (コア) の数です。
コレクターによって検出されました。専用のマーカー スレッドは、
クライアントが`GC_start_mark_threads()`を呼び出すか、クライアントが最初の呼び出しを開始するとき
GC 初期化後 (または、フォーク操作後) の非メインスレッド
子プロセス）。このフラグのもう 1 つの効果は、より多くの機能に切り替えることです。
`GC_malloc_many`の同時実装により、フリー リストを
構築およびメモリは、複数のスレッドによって同時にクリアできます。
*`-D THREAD_LOCAL_ALLOC`を使用してコレクターをビルドすると、次のサポートが追加されます。
スレッドローカル割り当て。これにより、`GC_malloc`(実際には`GC_malloc_kind`) が発生します。
`GC_gcj_malloc`は、スレッドローカル割り当てを実行するために再定義されます。

スレッドローカル アロケータから返されるメモリは完全に交換可能です
標準のアロケータによって返される値を使用します。他の人が使用する可能性があります
スレッド。唯一の違いは、スレッドが十分なメモリを割り当てているかどうかです。
特定の種類のオブジェクトについては、スレッドローカルの空きリストを作成します。
種類を選択し、そこから割り当てます。これによりロックが大幅に軽減されます。スレッドローカル
空きリストは`GC_malloc_many`を使用して補充されます。

このフラグの重要な副作用は、デフォルトのフラグを置き換えることです。
スピン・ザ・スリープ・ロックは、スピン・ザ・キュー・ベースの実装に置き換えられます。
これにより、標準の割り当て関数の_パフォーマンスが低下_しますが、
通常、スレッドローカル割り当てが頻繁に使用される場合、パフォーマンスが向上します。
したがって、短期間のロック取得の数が大幅に減少します。

また、`USE_RWLOCK`マクロ (実験的) は、
ミューテックスからのアロケータ ロック実装ベース (場合は`CRITICAL_SECTION`)
Win32 の) を`pthread_rwlock_t`(それぞれ`SRWLOCK`) に変換し、有効化します。
可能な場合は、リーダー (共有) モードでスリム ロックを取得します。  見る
`GC_call_with_reader_lock`および`GC_REVEAL_POINTER`エンティティの説明
詳細については、`gc.h`ファイルを参照してください。

## 並列マーキング アルゴリズム

遠藤、田浦、米沢が開発したものと同様のアルゴリズムを使用します。
([大規模分散メモリ マシン上の並列プログラミング言語のための効果的なガベージ コレクション戦略](https://dl.acm.org/doi/pdf/10.1145/263767.263801))
東京大学で。ただし、データ構造と実装は
は異なり、元のコレクターに対する小さな変更を表します。
おそらく、極端なスケーラビリティが犠牲になります。そのうちのいくつかは、
彼らが提案する改良点、例:大きなオブジェクトの分割も組み込まれました
私たちのアプローチに取り入れてください。

グローバル マーク スタックはグローバル ワーク キューに変換されます。とは異なり、
通常の場合、マークフェーズ中に縮小することはありません。マークスレッドが削除されます
オブジェクトをローカル マーク スタックにコピーし、
グローバル記述子をゼロに設定し、これ以上行うべき作業がないことを示します
このエントリーに関しては。この削除は同期なしで行われます。したがって、それは
複数のワーカーが同じエントリを削除する可能性があり、その結果、
作業の重複。

グローバル ワーク キューは、マーカー スレッドが一部のワーク キューを返すことを決定した場合にのみ増加します。
ローカル マーク スタックをグローバル マーク スタックに変換します。これは、グローバル キューが存在する場合に実行されます。
が不足しているように見える場合、またはローカル スタックがオーバーフローする危険がある場合。
同期は必要ですが、頻度は比較的低いはずです。

シーケンシャル マーキング コードは、ローカル マーク スタックを処理するために再利用されます。したがって、
並列マーキングに必要な追加コードの量は最小限です。

増分/世代別コレクションを使用できる必要があります。
`GC_enable_incremental`を呼び出すことでパラレル コレクターを存在させることができますが、
現在の実装では並列マーカーの中断は許可されていません。
したがって、クライアントが収集時間制限を設定している場合、後者はほとんど回避されます。

Gcj スタイルのマーク記述子は現在ローカルの組み合わせと混合しません。
割り当てと増分収集。これらはいずれか 1 つで正しく動作するはずです。
もう一方ですが、両方ではありません。

マーカー スレッドの数は、起動時に使用可能な数に設定されます。
プロセッサ コア (または`GC_MARKERS`または`GC_NPROCS`のいずれかの値)
環境変数 (指定されている場合)。単一のプロセッサのみが検出された場合、
平行マーキングは無効になります。

`GC_NPROCS`環境変数を 1 に設定すると、次のような問題が発生することに注意してください。
コレクター内で取得をロックしてプロセッサーをすぐに解放します。
最初に忙しく待つ代わりに。マルチプロセッサとクライアントの場合
同時に実行可能な複数のスレッドでは、悲惨な結果が生じる可能性があります
パフォーマンスへの影響 (例: 10 分の 1 の速度低下)。

＃＃ パフォーマンス

私たちは、次の亜種を使用していくつかの簡単な実験を行いました。
[GC ベンチマーク](http://www.hboehm.info/gc/gc_bench/)
同じアドレス空間で複数のクライアント スレッドを同時に実行するように変更されました。
各クライアント スレッドは元のベンチマークと同じ作業を実行しますが、
山を共有します。このベンチマークにはメモリ以外の作業はほとんど含まれません
割り当て。これはデュアル上の古い GC (2000 年にリリース) で実行されました。
Linux 2.2.12 上のプロセッサ Pentium III/500 マシン。

スレッドアンセーフ コレクターを使用して実行すると、ベンチマークは 9 秒で実行されました。と
`-D GC_THREADS`で構築されたシンプルなスレッドセーフ コレクター、実行
時間は 10.3 秒、つまり 2 つのクライアントの場合は 23.5 秒に増加しました。 (
`malloc`/`free`バリアントと`glibc``malloc`の時間は 10.51 です。
(標準ライブラリ、`pthreads`はリンクされていません)、20.90 (1 つのスレッド、`pthreads`)
はリンクされています）、および 24.55 秒です。ベンチマークはゴミに有利
ほとんどのオブジェクトは小さいため、コレクター。)

次の表は、次のコマンドを使用して構築されたコレクターの実行時間を示します。
並列マーキングとスレッドローカル割り当てのサポート
(`-D GC_THREADS -D PARALLEL_MARK -D THREAD_LOCAL_ALLOC`)。クライアントをテストしました
1 つまたは 2 つのマーカー スレッドを使用し、1 つまたは 2 つのクライアント スレッドを実行します。
クライアントはスレッドローカル割り当てのみを使用することに注意してください。と
`-D THREAD_LOCAL_ALLOC`コレクターはロック戦略に切り替えます。
ロックの取得頻度が少なくなるように調整されています。標準的な配分
したがって、プリミティブは`-D THREAD_LOCAL_ALLOC`を使用しない場合よりもパフォーマンスがわずかに低下します。
タイムクリティカルなコードでは避けるべきです。

(アロケータの取得に`pthread_mutex_lock`を直接使用した結果
少なくとも古いバージョンの LinuxThreads では、ロックはさらに悪かったでしょう。
`-D THREAD_LOCAL_ALLOC`を使用して、最初にアロケータの取得を繰り返し試行します。
`pthread_mutex_try_lock`でロックし、試行間でビジー待機状態になります。後
試行回数が固定されている場合は、`pthread_mutex_lock`を使用します。)

これらの測定では、増分収集は使用されず、プリフェッチも使用されませんでした。
マーカーで有効になります。ベンチマークの C バリアントを使用しました。全て
測定値は、ロードされていないマシンでの経過秒数です。

クライアント スレッドの数| 1 マーカー スレッド (秒)| 2 マーカー スレッド (秒)
---|------|-----
1| 10.45| 7.85
2| 19.95| 12.3

シングルスレッドの場合の実行時間は、シングルスレッドの場合よりもわずかに悪くなります。
簡単なロック。ただし、シングルスレッドのベンチマークでも、
2 番目のプロセッサが利用可能な場合は、スレッドアンセーフなバリアントも同様です。の
スレッドローカル割り当て時間による 2 つのクライアントの実行時間はわずか 1.4
スレッドアンセーフでの単一スレッドのシーケンシャル実行時間の倍
たとえクライアントの作業が 2 倍必要であっても、それが表すのは
古いバージョンの 2 クライアントの場合に比べて 2 倍近く改善されました。
コレクタ。古いコレクターは明らかにまだ何らかの争いに苦しんでいます
ロックスキームがかなりうまく機能していたにもかかわらず、オーバーヘッド
調整した。

完全な線形高速化 (つまり、1 つのクライアントで 1 つのクライアントの実行時間は同じ)
プロセッサを 2 つのプロセッサ上の 2 つのクライアントとして使用することはおそらくこれでは実現できません
このような少数のプロセッサを備えたハードウェアであっても、メモリは
システムはガベージ コレクターにとって大きな制約であり、通常はプロセッサーが
単一のメモリ バスを共有するため、総メモリ帯域幅は変化しません。
プロセッサ（コア）の数に比例して増加します。

これらの結果は、ハードウェアと OS の両方の問題に非常に影響される可能性があります。
古い Pentium Pro マシンを使用した予備実験。
カーネルはそれほど期待できるものではありませんでした。
