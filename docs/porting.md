# Conservative Garbage Collector の移植手順

コレクターは比較的簡単に移植できるように設計されていますが、移植はできません。
コード自体。コレクターは本質的に次のような操作を実行する必要があります。
スタックのスキャンなどですが、移植可能な C コードでは不可能です。

以下のすべては、コレクターが移植されていることを前提としています。
バイトアドレス指定可能な 32 ビットまたは 64 ビットのマシン。現在、すべての成功したポート
64 ビット マシンへの移行には、LP64 および LLP64 ターゲット (特に Win64) が含まれます。あなた
これにより、バイトアドレス不可能なポートへのポートの試行は推奨されません。
8 ビット、または 16 ビット マシン。

コレクターの移植の難易度は、必要な要件によって大きく異なります。
機能性。最も単純なケースでは、いくつかの小さな追加のみが必要です。
`include/private/gcconfig.h`ファイル。これについては以下で説明します
セクション。後のセクションでは、いくつかのオプション機能について説明します。
より多くの移植作業が必要になります。

コレクターは`ifdef`を多用することに注意してください。他のものとは異なります
ソフトウェア プロジェクトでは、これが望ましいと繰り返し結論付けています。
システム依存のファイルにコピーされ、ファイル間でコードが重複します。しかし、
これを管理しやすくするために、`ifdef`をインデントすることを強く信じています。
(歴史的な理由により、通常は先頭にシャープ記号がありません)。
(コードにならない場合は、個別のソース ファイルでももちろん問題ありません
複製。）

## gcconfig.h へのプラットフォームの追加

マルチスレッドのサポートもダイナミック ライブラリ データのトレースもサポートされていない場合は、
必要な場合は、多くの場合、必要な変更はこれらだけです。

`include/private/gcconfig.h`ファイルは 3 つのセクションで構成されます。

1. アーキテクチャを識別する GC 内部マクロを定義するセクション
(例:`IA64`または`I386`) およびオペレーティング システム (例:`LINUX`または`MSWIN32`)。
これは通常、事前定義されたマクロをテストすることによって行われます。自分たちで定義することで、
定義済みマクロを直接使用する代わりに、少し強制することができます。
一貫性が向上し、コンパイラからある程度分離されます。
違い。ここに新しいエントリを追加するのは比較的簡単です。
ただし、既存のコードとの一貫性を保つようにしてください。特に、
一般的な 32 ビット アーキテクチャの 64 ビット バリアントは、「として扱われません」
新しいアーキテクチャ。代わりに、64 ビット マシンを明示的にテストします。
それが重要な数少ない場所。 (ここでの注目すべき例外は`I386`です
および`X86_64`。これは部分的には歴史的なものですが、部分的には次の理由によって正当化されます。
おそらく、より充実したアーキテクチャと ABI が存在するという事実
ここでは、RISC バリアントとは異なります。) GNU ベースのシステムでは、
`cpp -dM -x c /dev/null`は、事前定義されたセットを生成するようです
マクロ。他の一部のシステムでは、「verbose」コンパイラ オプションによってこれが行われる場合があります。
またはマニュアルページにそれらがリストされている場合があります。

2. 少数のプラットフォーム固有のマクロを定義するセクション。
その後、コレクターによって直接使用されます。単純なポートの場合は、ここにあります
ほとんどの努力が必要です。以下にマクロについて説明します。
このセクションには、各アーキテクチャのサブセクションが含まれています (
適切な`#ifdef`)。通常、各サブセクションにはいくつかの内容が含まれます
アーキテクチャに依存する定義の後に、OS に依存するいくつかのセットが続く
定義し、これも`#ifdef`ディレクティブで囲みます。

3. で未定義のままになっているいくつかのマクロのデフォルトを記入するセクション。
前のセクションで説明したように、ほとんど必要のないその他のマクロを定義します。
新しいプラットフォームに合わせた調整。通常、これらに触れる必要はありません。
以前は完全にサポートされていなかった OS に移植する場合は、
定義に別の句を追加する必要がある可能性があります。
`GET_MEM`の。

次のマクロは、各アーキテクチャに対して正しく定義する必要があります。
オペレーティング·システム：

*`MACH_TYPE`- マシンを表す文字列に定義されます。
建築。これは、マクロを識別するために使用される単なるマクロ名です。
アーキテクチャですが、引用符で囲まれています。

*`OS_TYPE`- オペレーティング システム名を表す文字列に定義されます。
これはオペレーティング システムを識別するためのマクロ名にすぎませんが、
引用符で囲みます。

*`CPP_WORDSZ`- アドレス (単に「ワード」とも呼ばれる) サイズ (ビット単位)
プリプロセッサのテストに適した定数として、つまりキャストや
`sizeof`式。 32 ビットと 64 ビットの両方の ABI をサポートするプラットフォームの場合、
これは、現在の ABI に応じて条件付きで定義する必要があります。

*`CPP_PTRSZ`-`CPP_WORDSZ`に似ていますが、ポインターのサイズが異なります (また、
「ポインタ サイズのワード」と呼ばれます) 単位はビットです。ほとんどのプラットフォームでは、その値は
`CPP_WORDSZ`と同じです。

*`ALIGNMENT`- すべてのポインターが収まる最大の`n`として定義されます。
`n`バイト境界に配置されることが保証されます。それを定義すると、
1 は常に動作しますが、パフォーマンスは低下します。すべての最新の 32 ビット プラットフォームの場合、
これは 4 です。最新のすべての 64 ビット プラットフォームでは、これは 8 です。(かどうかは関係ありません)
x86 は最新のアーキテクチャとして適格です。ここではコンパイラと
OSに依存します。)

*`DATASTART`- メイン データ セグメントの始まり。コレクターは、
ルート ポインターの`DATASTART`と`DATAEND`の間のすべてのメモリをトレースします。
一部のプラットフォームでは、これを定数アドレスに定義できますが、
経験上、それは危険であることがわかっています。理想的には、リンカーは次のように定義します。
アドレスがデータの先頭であるシンボル (例:`_data`)
セグメント。場合によっては、値を次を使用して計算できます。
`GC_SysVGetDataStart`関数。`SEARCH_FOR_DATA_START`の場合は使用されません
マクロが定義されているか、動的ロードがサポートされている場合、および動的
読み込みサポートは、返される`GC_register_main_static_data()`を定義します。
間違い。

*`SEARCH_FOR_DATA_START`- これが定義されている場合、`DATASTART`が定義されます
から開始して取得される動的に計算された値に変換されます。
`_end`のアドレスとアドレス指定不可能なメモリまで逆方向に歩きます
が見つかりました。これは多くの場合、POSIX のようなプラットフォームで機能します。それはさらに難しくします
起動には生成とキャッチが含まれるため、クライアント プログラムをデバッグします。
ユーザーを混乱させる傾向があるセグメンテーション違反。

*`DATAEND`- メイン データ セグメントの終わりに設定します。デフォルトは`_end`です。
ここでそれは配列として宣言されます。これは場合によっては機能します。
リンカーは適切なシンボルを導入します。

*`DATASTART2`、`DATAEND2`- 一部のプラットフォームには 2 つの不連続なメイン データがあります。
セグメント、例:初期化されたデータと初期化されていないデータの場合。だとしたらこの二人
マクロは、2 番目のメイン データ セグメントの制限内で定義する必要があります。

*`STACK_GROWS_UP`- スタック (またはスレッド スタック) が増大した場合に定義する必要があります
より高いアドレスに向かって。 (これは PA-RISC でのみ当てはまるようです。
アーキテクチャにスレッドごとに複数のスタックがあり、そうでない場合
まだサポートされていますが、さらに作業を行う必要があります。`E2K`および`IA64`の grep
例としてソースコード内にあります。)

*`STACKBOTTOM`- スタックのコールドエンドとして定義され、通常は
(つまり、スタックが成長したとき) スタック内の最上位のアドレス。
ポインタを含むスタックの領域をバインドする必要があります。
コレクターの山。マルチスレッドのサポートでは、これがコールドエンドである必要があります
メインスタックの。通常は、
他のスレッドスタック。これが定義されておらず、次のいずれでもない場合
3 つのマクロ (`SPECIFIC_MAIN_STACKBOTTOM`、`HEURISTIC1`、`HEURISTIC2`)
が定義されている場合、クライアント コードは`GC_stackbottom`を明示的に設定する必要があります。
`GC_INIT`または他の`GC_`ルーチンを呼び出す前に、適切な値を設定してください。

*`SPECIFIC_MAIN_STACKBOTTOM`-`STACKBOTTOM`の代わりに定義できます。
定義されている場合、メインスタックのコールドエンドは何らかの方法で決定されます。
OS 固有の方法、例: Linux の場合は`/proc`から読み取ります。

*`HEURISTIC1`-`STACKBOTTOM`の代わりに定義できます。 __インラインコード2__
この場合も定義する必要があります。スタックのコールドエンドは
`GC_init`s フレーム内のアドレスを取得し、それを四捨五入することによって決定されます。
`STACK_GRAN`の次の倍数まで。これはスタックの場合にうまく機能します
底部は常に 2 の大きな累乗に揃えられます。 (注: 定義
`STACK_GRAN`から 0x1000000 が最適であることはほとんどありません。)

*`HEURISTIC2`-`STACKBOTTOM`の代わりに定義できます。冷たい終わり
スタックの値は`GC_init`s フレーム内のアドレスを取得することによって決定されます。
小さなステップで繰り返し増加します (`STACK_GROWS_UP`の場合は減少します)。
そして各位置の値を読み取ります。値を覚えているのは、
最初のセグメンテーション違反またはバス エラーが通知された場合、それを次の値に丸めます。
最も近い妥当なページ境界を選択し、それをスタックの一番下として使用します。

*`DYNAMIC_LOADING`-`dyn_load.c`ファイルが既に作成されている場合に定義する必要があります。
このプラットフォーム用に更新され、ダイナミック ライブラリのルートのトレースが行われます。
サポートされました。

*`GWW_VDB`、`MPROTECT_VDB`、`PROC_VDB`、`SOFT_VDB`、`UFFDWP_VDB`- 可能性があります
対応する_仮想ダーティ ビット_実装が
`os_dep.c`ファイルはこのプラットフォームで使用できます。これにより、
増分/世代別ガベージ コレクション。 (`GWW_VDB`は Win32 を使用します
ダーティビットを読み取るための`GetWriteWatch`関数、`MPROTECT_VDB`および
`UFFDWP_VDB`ヒープを書き込み保護することで変更されたページを識別し、
障害をキャッチするには、`PROC_VDB`および`SOFT_VDB`が`/proc`疑似ファイルを使用して、
ダーティビットを読み取ります。)

*`PREFETCH`、`GC_PREFETCH_FOR_WRITE`- コレクターは`PREFETCH(x)`を使用します
`x`アドレスのデータをキャッシュにプリロードします。これはデフォルトで
ノーオペ。

*`CLEAR_DOUBLE`- 定義されている場合、`CLEAR_DOUBLE(x)`が次のように使用されます。
`GC_malloc`で整列された 2 つの「ポインターサイズ」の単語をクリアする高速な方法
アドレス`x`。デフォルトでは、代わりに`NULL`ポインターの保存が使用されます。

*`HEAP_START`- の初期アドレス ヒントとして定義できます。
`mmap`ベースの割り当て。

## 基本ポートの追加要件

場合によっては、プラットフォーム固有のコードを他のコードに追加する必要がある場合があります。
ファイル。考えられる候補は次の実装です。
`mach_dep.c`ファイル内の`GC_with_callee_saves_pushed`。これにより、登録が確実に行われます
コレクターがトレースする必要があるコンテンツがスタックにコピーされます。通常
これは移植可能に行うことができますが、一部のプラットフォームではアセンブリ コードが必要になる場合があります。
または、条件付きコンパイル テストを微調整するだけです。

プラットフォームが`getcontext`をサポートしている場合は、マクロ`UNIX_LIKE`を定義します。
`include/private/gcconfig.h`ファイル内の OS (まだ定義されていない場合)
問題を解決できる可能性があります。それ以外の場合、gcc を使用している場合は、
`_builtin_unwind_init`が使用され、正常に動作するはずです。そうでない場合
どちらにも該当する場合、実装は`setjmp`の使用を試みます。これはうまくいきます
`setjmp`実装がポインタ値の可能性のあるすべてのレジスタを保存する場合
`longjmp`時にスタックを巻き戻そうとするのではなく、バッファに格納します。
`setjmp_test`テストはこれを判断しようとしますが、多くの場合、判断できません。
右。アセンブリコードで処理されるレジスタトレースは、一般的に次のようになります。
避けられた。

ほとんどの場合、`os_dep.c`ファイルには注意する必要はありませんが、以下を参照してください。

## マルチスレッドのサポート

スレッドをサポートするには、コレクターがすべてのスレッドを見つけて一時停止できる必要があります。
ガベージ コレクション ヒープにアクセスしている可能性のあるスレッドを調べ、状態を特定します。
トレースする必要がある各スレッドに関連付けられています。

マルチスレッドのサポートに必要な機能は一般的に実装されています
特定のスレッド インターフェイスに固有の 1 つ以上のファイル内。例えば。、
`pthreads`の移植性の高いサポートが`pthread_support.c`に実装されています
および`pthread_stop_world.c`ファイル。重要な機能は次のもので構成されます。

*`GC_stop_world`- ガベージ コレクションされたファイルにアクセスする可能性のあるすべてのスレッドを停止します。
呼び出し元以外のヒープ。
*`GC_start_world`- 他のスレッドを再起動します。
*`GC_push_all_stacks`- すべてのスレッド スタックの内容をプッシュします (または、
少なくとも、スレッドスタック内のポインタを含む領域の) をマークに乗せます
スタック。

多くの場合、ガベージ コレクターが独自のデータを維持する必要があります。
アクティブなスレッドを追跡する構造。

さらに、`LOCK`と`UNLOCK`を実装する必要があります。
`include/private/gc_locks.h`ファイル。

最も簡単なケースは、おそらく、スレッドを実行できる新しい`pthreads`プラットフォームです。
信号で止まった。この場合、変更には以下が含まれます。

1.`GC_THREADS`マクロがビルド スクリプトによって定義されている場合に、
その結果、`GC_PTHREADS`が定義されます。
`include/private/gcconfig.h`ファイル。

2.`libatomic_ops`パッケージが少なくとも最低限のサポートを保証する
プラットフォーム。インクリメンタル GC が必要な場合、または`pthreads`ロックが必要ない場合
アロケータ ロックとして適切に機能するには、おそらく次のことが必要になります。
プラットフォームに十分な`libatomic_ops`ポートが存在することを確認してください。
アトミックなテストアンドセット操作を提供しました。あるいは、コレクター
`libatomic_ops`パッケージの代わりに GCC アトミック組み込み関数を使用できます (「
`include/private/gc_atomic_ops.h`ファイル)。

3.`pthread_stop_world.c`に必要な調整を加え、
`pthread_support.c`ファイル。理想的には何も必要ありません。実際にはそうではありません
これらすべては希望どおりに標準化されており、完全なバグです
回避策が必要になることはよくあります。  非プリエンプティブ スレッド パッケージは、
おそらくさらなる作業が必要です。同様にスレッドローカル割り当てと
並列マーキングには`pthread_support.c`ファイルでさらに作業が必要です。
設計されたプラットフォームに対して、より優れた`libatomic_ops`サポートが必要になる場合があります。

## 動的ライブラリのサポート

`DATASTART`と`DATAEND`が正しく定義されている限り、コレクターは
ファイル スコープまたは一部として定義された`static`変数から到達可能なトレース メモリ
メインの実行可能ファイル。プログラムが静的である場合、これで十分です。
リンクされている場合、またはガベージ コレクション ヒープへのポインタが格納されない場合
動的ライブラリで定義された非スタック変数。

ダイナミック ライブラリ データ セクションもトレースする必要がある場合は、次のようにします。

*`DYNAMIC_LOADING`は、適切なセクションで定義する必要があります。
`include/private/gcconfig.h`ファイル。
* 関数`GC_register_dynamic_libraries`の適切なバージョン
`dyn_load.c`ファイルで定義する必要があります。この関数は呼び出す必要があります
各ダイナミックの`GC_cond_add_roots(region_start, region_end, TRUE)`
ライブラリデータセクション。

書き込み可能なデータセグメントをスキャンする実装はエラーが発生しやすく、
特にスレッドが存在する場合。それらは頻繁に人種差別を引き起こす
スレッドが終了してスタックが消滅するときの条件。彼らはまた、偶然に
グラフィックス メモリの大きな領域またはマップされたファイルをトレースします。少なくとも 1 つでは
場合によっては、追跡できないデバイスのメモリを追跡しようとすることが知られています。
GC が読み取りたい方法で安全に読み取られます。

通常、特に動的リンカーのデータ構造をたどるほうが安全です。
リンカがインターフェイスをエクスポートする場合。ただし、文書化が不十分な場合は注意してください
この場合のロック動作。

## インクリメンタル GC サポート

増分および世代別コレクションが機能するには、`os_dep.c`ファイルが必要です。
適切な_仮想ダーティ ビット_実装が含まれており、これにより、
どのヒープ ページ (ヒープ ページの倍数であると想定される) を追跡するコレクター。
コレクターのブロック サイズ) が一定の時間間隔で書き込まれました。
コレクターは、適応される可能性のあるいくつかの実装を提供します。
デフォルト (`DEFAULT_VDB`) は、すべてのページを次のように扱うプレースホルダーです。
書かれていること。これにより正確さが保証されますが、増分レンダリングと
世代間のコレクションは本質的に役に立たない。

## デバッグサポートのためのスタックトレース

デバッグ サポートにオブジェクト内のスタック トレースが必要な場合は、`GC_save_callers`と
`GC_print_callers`を実装する必要があります。

## 免責事項

これは移植ガイドラインの最初のパスです。いくつかのことは疑いの余地がありません
見落とされた。
