# 保守的な GC アルゴリズムの概要

これは、私たちのアルゴリズムとデータ構造の説明です。
保守的なガベージコレクター。詳細レベルはさらに上がると思います
時間。 GC アルゴリズムの調査の場合、例:ポール・ウィルソンの作品を参照
[ユニプロセッサのガベージ コレクション技術](https://ftpmirror.infania.net/sites/ftp.leo.org/history/doc/programming/gcsurvey.ps.Z)
優れた紙。コレクター インターフェイスの概要については、を参照してください。
[ここ](gcinterface.md)。

この説明は主に、次のことを理解しようとしている人を対象としています。
ソースコード。特に変数名と関数名を指します。かもしれない
アルゴリズムをより高いレベルで理解するのにも役立ちます。

ここでの説明は、コレクターがデフォルト モードで使用されることを前提としています。
特に、単なるガベージ コレクターとしてではなく、ガベージ コレクターとして使用されることを想定しています。
漏れ検知器。当初はストップ・ザ・ワールドでの使用を想定していましたが、
非増分モードですが、増分コレクタが存在すると、
デザインに顕著に現れます。デフォルトのファイナライゼーション モデルを想定していますが、
その影響を受けるコードは非常にローカライズされています。

目次
* [はじめに](#はじめに)
* [割り当て](#allocation)
* [マークフェーズ](#mark-phase)
* [スイープフェーズ](#スイープフェーズ)
* [ファイナライズ](#finalization)
* [世代別コレクションとダーティ ビット](#generational-collection-and-dirty-bits)
* [ブラックリスト](#black-listing)
* [マルチスレッドのサポート](#multi-threading-support)
* [スレッドローカル割り当て](#thread-local-allocation)

＃＃ 導入

ガベージ コレクターは、修正されたマークスイープ アルゴリズムを使用します。 Conceptually
それは大まかに 4 つのフェーズで動作し、一部として時々実行されます。
メモリ割り当ての:

1. _Preparation phase_.各オブジェクトには、関連付けられたマーク ビットがあります。すべてクリア
マークビットは、すべてのオブジェクトが到達不能である可能性があることを示します。

2. _マークフェーズ_。のチェーンを介して到達可能なすべてのオブジェクトをマークします。
変数からのポインタ。多くの場合、コレクターは本当の情報を持っていません
ヒープ内のポインター変数の位置について、すべてを表示します。
静的データ領域、スタック、およびレジスターが含まれる可能性がある
ポインタ。ヒープ オブジェクト内のアドレスを表すビット パターン
コレクターによって管理されるものはポインターとして見なされます。クライアントでない限り、
プログラムにより、ヒープ オブジェクト レイアウト情報が利用可能になりました。
コレクターでは、変数から到達可能であることが判明したヒープ オブジェクトはすべて
再度同様にスキャンしました。

3. _スイープフェーズ_。ヒープをスキャンして、アクセスできない、つまりマークされていないものを探します。
オブジェクトを取得し、それらを再利用のために適切な空きリストに返します。これは
実際には別のフェーズではありません。非インクリメンタルモードでもこの操作は
通常、割り当て中にオンデマンドで実行され、
空のフリーリスト。したがって、スイープフェーズがページに触れる可能性は非常に低いです。
とにかくその後すぐには触れられなかったでしょう。

4. _最終段階_。登録されていた到達不能なオブジェクト
ファイナライゼーションは、コレクターの外部でファイナライゼーションのためにキューに入れられます。

残りのセクションでは、メモリ割り当てデータ構造について説明します。
次に、最後の 3 つの収集フェーズについて詳しく説明します。概要を説明して終わります
コレクターに実装された追加機能の一部。

## 割り当て

コレクターには独自のメモリ アロケーターが含まれています。アロケータはメモリを取得します
プラットフォームに依存した方法でシステムから取得します。 UNIX では、次のいずれかを使用します。
`malloc`、`sbrk`、または`mmap`。

アロケータによって使用されるほとんどの静的データと、残りの部分で必要なデータ
ガベージ コレクターのデータは`_GC_arrays`構造体内に保存されます。これ
ガベージ コレクターがコレクター自身のデータを簡単に無視できるようにします。
ルート ポインターを検索するときの構造体。その他のアロケーターとコレクター
内部データ構造は`GC_scratch_alloc`を使用して動的に割り当てられます。
後者は割り当て解除を許可しないため、次の目的でのみ使用されます。
永続的なデータ構造。

アロケータは、異なる種類のオブジェクトを返します。さまざまな_種類_があります
ガベージ コレクターの特定の部分では多少異なる方法で処理されます。
特定の種類ではポインターがスキャンされますが、他の種類ではスキャンされません。持っている人もいるかもしれません
ポインタの位置を決定するオブジェクトごとのタイプ記述子。または特定の
kind は、1 つの特定のオブジェクト レイアウトに対応する場合があります。内蔵されているのは2種類
回収不能。それにもかかわらず、ほとんどの C クライアントは
コレクターは現在、`NORMAL`オブジェクトと`PTRFREE`オブジェクトの 2 種類を使用します。の
[gcj](https://gcc.gnu.org/onlinedocs/gcc-4.8.5/gcj/) ランタイムも重くなる
型情報を格納する種類 (`GC_gcj_malloc`で割り当てられた) の使用
メソッド テーブル内の既知のオフセットにあります。

コレクターは 2 レベルのアロケーターを使用します。大きなブロックは 1 つとして定義されます
`HBLKSIZE`の半分 (2 の累乗) より大きく、通常は次のとおりです。
ページサイズのこと。

大きなブロック サイズは、`HBLKSIZE`の次の倍数に切り上げられてから、
`GC_allochblk`によって割り当てられます。コレクターは近似の最適値を使用します
いくつかの大きなブロック サイズの空きリストを保持することにより、アルゴリズムを強化します。実際の
`GC_allochblk`の実装はブラックリスト化により大幅に複雑になります
問題があります (下記を参照)。

小さなブロックは、`HBLKSIZE`サイズのチャンクに割り当てられます。各チャンク
は、1 つのオブジェクトのサイズと種類のみに特化しています。

アロケータは、オブジェクトのサイズと種類ごとに個別の空きリストを維持します。
それぞれの種類に関連付けられているのは、エントリを含むフリーリスト ポインターの配列です。
サイズ`i`オブジェクトの空きリストを指す`freelist[i]`。インデックス`i`は
割り当て可能な最小単位である顆粒で表され、通常は 8 または
16バイト。フリー リスト自体は、次の最初のポインタを介してリンクされています。
各オブジェクト (`obj_link`マクロを参照)。

大きなブロックが小さなオブジェクトで使用するために分割されると、それはのみ使用できます。
コレクターが完全に空のオブジェクトを発見しない限り、そのサイズのオブジェクトの場合
かたまり。完全に空のチャンクは適切な大きなブロックに復元されます
無料リスト。

あまりにも多くの個別のオブジェクト サイズにブロックが割り当てられるのを避けるために、
コレクターは通常、可能なすべてのオブジェクトを直接割り当てません。
リクエストサイズ。代わりに、リクエストはより小さい数値の 1 つに切り上げられます。
割り当てられたサイズの空きリストが維持されます。正確に割り当てられた
サイズはオンデマンドで計算されますが、増加するという制約が適用されます。
ほぼ等比数列です。したがって、オブジェクトは早い段階でリクエストされました。
実行は要求されたサイズと正確に割り当てられる可能性が高く、サブジェクト
整列制約に適用されます。詳細については、`GC_init_size_map`を参照してください。

小さなオブジェクト割り当て時の実際のサイズの丸め操作
`GC_size_map`のテーブル ルックアップとして実装され、リクエストされた
顆粒の数に対するバイト単位の割り当てサイズ。

コレクターの初期化と割り当てサイズの計算の両方が処理されます
小さなオブジェクトの高速割り当てパスが遅くならないように注意してください。
コレクターが初期化される前、または
適切な`GC_size_map`エントリが計算され、エントリと同じパスをたどります。
空の空きリストを使用して割り当てを試行しました。これにより、低速の呼び出しが行われます。
適切な処理を実行するパス コード (`GC_generic_malloc_inner`)
初期化チェック。

非増分モードでは、ガベージ コレクションを行うかどうかを決定します。
現在のヒープ サイズでは割り当てが失敗するたびに。
最後の収集以降の割り当ての合計量が
ヒープ サイズを`GC_free_space_divisor`で割って、ヒープを拡張しようとします。
それ以外の場合は、ガベージ コレクションが開始されます。これにより、金額が保証されます
割り当てられたバイトごとのガベージ コレクション作業量は一定のままです。

実際、上記は実際のヒープ拡張と GC を過度に単純化したものです。
トリガーヒューリスティック。ルートのサイズと特定の種類に合わせてわずかに調整します。
断片化の。特に：

* ルート セット サイズが大きく、ライブ ヒープ メモリが少ないプログラムは、
ルートをスキャンするコストを償却するためにヒープを拡張します。
* GC は実際には、非増分モードでより頻繁に収集します。大きな
ブロック アロケーターは通常、ガベージが発生すると大きなヒープ ブロックを分割することを拒否します。
収集しきい値に達しました。これは多くの場合、収集効果をもたらします。
ヒープがいっぱいになるかなり前に、断片化とワーキング セットが削減されます。
GC 時間を犠牲にしてサイズを大きくします。 (コレクターがマップを解除しないように構成されている場合
未使用のページがある場合、GC はその量に応じて中間戦略を選択します。
大きなオブジェクトの割り当ては過去に行われました。)
* 前回のコレクション以降の割り当て量を計算する際に、
明示的に割り当てが解除されることが予想されるオブジェクトの部分的なクレジット。平
すべてのオブジェクトが明示的に管理されている場合は、多くの場合、収集することが望ましいです。
まれに、それが完全に合体するための唯一のメカニズムであるため、
空のチャンク。

拡張を優先するようにこれを調整する必要があると提案されています。
結果のヒープがまだ物理メモリに収まるかどうか。多くの場合、それは
間違いなく役立つでしょう。しかし、これを堅牢な方法で行うのは困難です
複数のアプリケーションが単一の物理メモリ プールをめぐって競合している場合。

## マークフェーズ

各コレクションでは、コレクターは、可能性のあるすべてのオブジェクトにマークを付けます。
ポインタ変数からアクセス可能。一般にポインタがどこにあるかを知ることができないため、
変数が見つかると、次の _root セグメント_ をスキャンしてポインターを探します。

* レジスター。アーキテクチャに応じて、これは次を使用して実行できます。
アセンブリ コード、またはレジスタを保存する`setjmp`のような関数を呼び出すことによって
スタック上の内容。

* スタック。シングルスレッド アプリケーションの場合、ほとんどのアプリケーションでは
これは、プラットフォーム間でメモリをスキャンすることによって行われます（近似値）。
現在のスタック ポインターと`GC_stackbottom`。 (インテル Itanium の場合、
レジスタ スタックは個別にスキャンされます。)`GC_stackbottom`変数は、
適切な構成に応じたプラットフォーム固有の方法
`include/private/gcconfig.h`ファイル内の情報。現在、
アクティブなスタックは注意深くスキャンする必要があります。
クライアント コードはコレクター スタック フレーム内に表示される場合があり、変更される可能性があります。
マーク処理中。これは、のいくつかのセクションをスキャンすることで解決されます。
_熱心に_スタックし、ある時点でのスナップショットを効果的にキャプチャします。

* 静的データ領域。最も単純なケースでは、これは次の間の領域です。
`DATASTART`および`DATAEND`(`include/private/gcconfig.h`ファイルで定義)。
ただし、ほとんどの場合、これには静的データ領域も含まれます。
動的ライブラリに関連付けられています。これらは主に次の方法で識別されます。
`dyn_load.c`ファイル内のプラットフォーム固有のコード。  マーカーは、
アクセス可能であることがわかっているメモリ領域の明示的なスタック。
含まれているポインタがまだ検索されていません。各スタック エントリには次の内容が含まれます
スキャンされるブロックの開始アドレスと、その記述子
ブロック。ブロックのレイアウト情報が利用できない場合、
記述子は単なる長さです。 (他の可能性については、`gc_mark.h`を参照してください。
ファイル。）

マークフェーズの開始時に、すべてのルートセグメント (上で説明したように) が
`GC_push_roots`によってスタックにプッシュされます。 (レジスターと熱心にスキャンされたスタック
セクションは、スタックの代わりに参照されたオブジェクトをプッシュすることによって処理されます。
セクション自体。)`ALL_INTERIOR_POINTERS`が定義されていない場合、スタック ルート
特別な治療が必要です。この場合、通常のマーキング コードは無視します。
内部ポインターですが、`GC_push_all_stack`は内部を明示的にチェックします
ターゲット オブジェクトのポインターとプッシュ記述子。

マーカーは増分マーキングを可能にする構造になっています。それぞれの通話
to`GC_mark_some`は、ヒープをマークするための少量の作業を実行します。
`GC_mark_state`の形式で明示的な状態を維持します。
特定のサブフェーズ。他のいくつかの状態、最も顕著なのはマーク
スタックを作成し、各サブフェーズで実行すべき作業がどれだけ残っているかを特定します。通常の
stop-the-world コレクションのマーク状態の進行は次のとおりです。

1.`MS_INVALID`は、マークされていないアクセス可能なオブジェクトがある可能性があることを示します。
この場合、`GC_objects_are_marked`は同時に false になるため、
マークの状態が次の状態に進みます

2.`MS_PUSH_UNCOLLECTABLE`は、回収不能をプッシュするだけで十分であることを示します
オブジェクト、ルートを設定し、そこから到達可能なすべてのものにマークを付けます。
`GC_scan_ptr`は、すべてが収集できなくなるまでヒープ内を進められます。
オブジェクトがプッシュされ、そこから到達可能なオブジェクトがマークされます。そこで
ポイント、`GC_mark_some`への次の呼び出しは`GC_push_roots`を呼び出して、
ルーツ。次に、マークの状態を次のように進めます。

3.`MS_ROOTS_PUSHED`は、マーク スタックが空になると、すべてのマーク スタックが空になることをアサートします。
到達可能なオブジェクトにはマークが付けられます。この状態になったら、次の作業のみを行います。
マークスタックを空にします。これが完了すると、状態は次のように変わります。

4.`MS_NONE`は、到達可能なオブジェクトがマークされていることを示します。

コア マーク ルーチン`GC_mark_from`は、いくつかのプログラムによって繰り返し呼び出されます。
マークスタックが埋まり始めるとサブフェーズになります。繰り返し呼ばれることもあります
`MS_ROOTS_PUSHED`状態でマーク スタックを空にします。ルーチンは設計されています
各呼び出しで限られた量のマーキングのみを実行するため、
インクリメンタルコレクターでも使用されます。かなり丁寧に調整されていて、
これは通常、ガベージ コレクション時間の大部分を消費するためです。

呼び出しごとに少量の作業しか実行しないという事実により、
これは、並列マーカーのコア ルーチンとして使用されます。その場合は、
通常はグローバル マークではなくスレッド プライベート マーク スタックで呼び出されます
スタック。詳細については、[こちら](scale.md) をご覧ください。

マーカーはマーク スタックのオーバーフローを正しく処理します。マークがスタックするたびに
オーバーフローすると、マークの状態は`MS_INVALID`にリセットされます。すでにあるので、
ヒープ内のマークされたオブジェクトの完全なスキャンが最終的に強制されます。
ヒープ、ポインターの検索、その間にマークされていないオブジェクトが参照される
マークされたオブジェクトは再びマーク スタックにプッシュされます。このプロセスが繰り返される
スタック オーバーフローが発生することなくマーク フェーズが完了するまで。スタックするたびに
オーバーフローすると、マーク スタックを拡張しようとします。のすべての部分
リージョンをマーク スタックにプッシュするコレクターは、次の点に注意する必要があります。
マークスタックのオーバーフローが繰り返された場合でも、前方に進みます。すべてのマーク
試みると、追加のマークされたオブジェクトが生成されます。

各マーク スタック エントリは、
エントリで説明される範囲。領域に関連付けられた型情報がない場合
この場合、通常、各 4 バイトで整列された量 (8 バイトで整列された) が必要になります。
64 ビット ポインターの場合) は候補ポインターとみなされます。

候補ポインタが実際にヒープのアドレスであるかどうかを判断します
ブロック。これは次の手順で行われます。

* 候補ポインタは大まかなヒープ境界に対してチェックされます。これらのヒープ
境界は、すべての実際のヒープ オブジェクトが境界の間に収まるように維持されます。
ブラックリストに登録しやすくするために (以下を参照)、アドレスも含めます。
ヒープが拡張される可能性がある領域。ほとんどの非ポインターはこれに失敗します
初期テスト。
* 候補ポインタは 2 つの部分に分かれています。最も重要な
ビットはアドレス空間内の`HBLKSIZE`サイズのページを識別し、最小ビットはアドレス空間内の`HBLKSIZE`サイズのページを識別します。
有効ビットはそのページ内のオフセットを指定します。 (ハードウェア ページでは、
実際には、そのような複数のページで構成されます。通常、`HBLKSIZE`は通常、
ページ サイズを 2 の小さな累乗で割った値。あるいは、コレクターの場合は、
`-D LARGE_CONFIG`で構築されているため、そのようなページは複数のハードウェアで構成されている可能性があります
ページ。)
※候補ポインタのページアドレス部分は、
[テーブル](tree.md)。各テーブル エントリには、次のことを示すいずれかの 0 が含まれます。
ページはガベージ コレクション ヒープの一部ではなく、小さな整数`n`です。
ページが少なくとも`n`で始まるラージ オブジェクトの一部であることを示します
ページ戻る、またはページの記述子へのポインター。最初のケースでは、
候補ポインタは真のポインタではないため、無視しても安全です。
最後の 2 つのケースでは、以下を含むページの記述子を取得できます。
オブジェクトの始まり。
* 参照されたオブジェクトの開始アドレスが計算されます。ページ
記述子には、そのページ内のオブジェクトのサイズ、オブジェクトの種類、
およびそれらのオブジェクトに必要なマーク ビット。サイズ情報は次のとおりです。
候補ポインタをオブジェクトの開始アドレスにマップするために使用されます。
このプロセスを高速化するために、ページ ヘッダーには次のポインタも含まれています。
ページ オフセットからページの先頭からの変位への事前計算されたマップ。
物体。このマップを使用すると、整数の剰余が遅くなる可能性が回避されます。
オブジェクトの開始アドレスを計算する際の操作。
※対象オブジェクトのマークビットを確認・設定します。オブジェクトが
以前にマークが解除されていた場合、オブジェクトはマーク スタックにプッシュされます。記述子
ページ記述子から読み取られます。 (これは保存されている情報から計算されます
ページが最初に割り当てられるときの`GC_obj_kinds`内。)

マークフェーズの終了時に、残ったフリーリストのマークビットがクリアされます。
迷走ポインタにより空きリストが誤ってマークされた場合に備えて。

## スイープフェーズ

マークフェーズの終了時に、ヒープ内のすべてのブロックが検査されます。マークなし
ラージ オブジェクトは、ラージ オブジェクトの空きリストに直ちに返されます。それぞれ
スモール オブジェクト ページがチェックされ、すべてのマーク ビットがクリアされているかどうかが確認されます。そうであれば、
ページ全体がラージ オブジェクト空きリストに返されます。スモールオブジェクトページ
到達可能なオブジェクトを含むオブジェクトは、後でスイープするためにキューに入れられます。
ページに含まれる空きスペースが非常に少ないと判断します。この場合、
それ以上の調査は行われません。

この最初のスイープ パスは、ブロック自体ではなく、ブロック ヘッダーのみに触れます。
したがって、たとえ大部分のセクションであっても、大幅なページングは​​必要ありません。
ヒープは物理メモリ内にありません。

割り当て試行が発生すると、空ではないスモール オブジェクト ページがスイープされます。
そのオブジェクトのサイズと種類の空の空きリスト。正しいサイズのページ数
および kind は、少なくとも 1 つの空のブロックが見つかるまで繰り返しスイープされます。
このようなページをスイープするには、ページ ヘッダーのマーク ビット配列をスキャンする必要があります。
そしてオブジェクト内の最初のポインタを介してリンクされたフリーリストを構築します
彼ら自身。これには、適切なデータ ページをタッチする必要がありますが、ほとんどの場合、
場合によっては、割り当てに使用される直前にのみ触れられます。したがって、任意の
ページングは​​基本的に避けられません。

ポインタのないオブジェクトの場合を除いて、あらゆるオブジェクトが存在するという不変条件を維持します。
スモールオブジェクト空きリスト内のオブジェクトはクリアされます (リンクを除く)
分野）。したがって、小さなオブジェクトのスイープ ルーチンをクリアするのが負担になります。
オブジェクト。これには、誤って発生した場合に簡単に回復できるという利点があります
フリーリストにマークを付けることもできますが、他の手段でも処理できます。の
コレクターは現在、オブジェクトのクリアにかなりの時間を費やしています。
おそらくアプローチを再考する必要があります。ほとんどの構成では、
一般的な小さなオブジェクト サイズを処理するための特殊なスイープ ルーチン。私たち以来
ポインタごとに 1 つのマーク ビットを割り当てると、関連するマーク ビットを調べることが容易になります。
オブジェクト サイズがポインター サイズを均等に分割する場合、ビットをマークします。私たちも適当に
それぞれのケースで内側のスイープ ループを展開します。 (考えられるのは、
コンパイラでのプロファイルベースのプロシージャのクローン作成により、これが不要になる可能性があります
そして逆効果。これが適用される既存のコンパイラを私は知りません。)

小さなオブジェクト ページのスイープは、犠牲を払って完全に回避できます。
アロケータ内でマーク ビットを直接検査する方法。おそらくこれはもっと多いでしょう
各割り当て呼び出しで大量のリロードが必要になるため、コストがかかる
状態 (例: スイープされる次のオブジェクトのアドレス、マーク ビット テーブル内の位置)
その仕事ができる前に。現在のスキームではアロケーターが単純に保たれており、
スイーパーで便利な最適化が可能になります。

## ファイナライズ

`GC_register_disappearing_link`と`GC_register_finalizer`の両方に、
対応するハッシュ テーブルにリクエストを送信します。ハッシュテーブルは次のように割り当てられます。
メモリは収集されますが、ファイナライズ可能なオブジェクトへの参照は隠蔽されます。
コレクター。現在、ファイナライズリクエストは非増分的に処理されます
マークサイクルの終了時。

コレクターはファイナライズ可能なオブジェクトのテーブルを最初に渡します。
マークされていないオブジェクトの内容をマーク スタックにプッシュします。押した後
各オブジェクトごとにマーカーが呼び出され、そこから到達可能なすべてのオブジェクトがマークされます。の
オブジェクト自体は明示的にマークされていません。これにより、オブジェクトが
ファイナライザが依存するものは収集もファイナライズもされません。

オブジェクトからマークを付ける過程でオブジェクト自体がマークされた場合、
私たちはそのオブジェクトに関係するサイクルを発見しました。通常、この結果は次のようになります。
コレクターからの警告。このようなオブジェクトはファイナライズされていない可能性があります。
そうするのは危険です。の詳細な説明を参照してください。
[ファイナライゼーション セマンティクス](finalization.md)。

このプロセスの終了時にマークされていないオブジェクトがキューに追加されます。
ファイナライザーを実行できるオブジェクトの数。コレクターの構成に応じて、
ファイナライザーはキューからデキューされ、割り当て呼び出し中に暗黙的に実行されます。
またはユーザーのリクエストに明示的に応答して。 (前者は注意してください
残念ながら、これはデフォルトであり、一般に安全ではありません。ファイナライザの場合
同期を実行すると、デッドロックが発生する可能性があります。重要なファイナライザ
通常、同期を実行する必要があるため、別のものが必要になります。
コレクター構成。)

コレクターは、使用されるプロシージャーを置き換えるメカニズムを提供します。
オブジェクトをマークスルーします。これは、Java スタイルのサポートを提供するために使用されます。
順序付けされていないファイナライゼーション、および特定の種類のサイクルを無視します。それらの
仮想継承の C++ 実装から生じます。

## 世代別コレクションとダーティ ビット

基本的には、で説明されている同時世代の GC アルゴリズムを使用します。
[ほぼ並列のガベージ コレクション](http://www.hboehm.info/gc/papers/pldi91.ps.Z)、
ベーム、デマース、シェンカー著。

最も重要な変更点は、コレクターが常に実行を開始することです。
割り当てスレッド内。個別のガベージ コレクター スレッドはありません。 （もし
並列 GC が有効になっている場合、ヘルパー スレッドも起動される可能性があります。)
大きなオブジェクトをリクエストするか、空の小さなオブジェクトに遭遇します。
フリーリストを確認し、進行中のコレクションがあることに気づくと、すぐに
上で説明したように、少量のマーキング作業を実行します。

この変更は、両方に簡単に対応したかったために行われました。
シングルスレッド環境では、別個の GC スレッドには非常に多くの処理が必要になるため、
ミューテーターが実行速度を超えないように、スケジューラーを注意深く制御します。
コレクターであるため、不必要なヒープの増加を引き起こします。

インクリメンタル モードでは、ヒープは常に拡張されます。
割り当てのためのスペースが不十分です。ガベージ コレクションは常にトリガーされます
`GC_heap_size / 2 * GC_free_space_divisor`バイトを超えていることがわかります
の割り当てが行われました。`GC_full_freq`マイナー コレクションの後、メジャー
収集が開始されます。

すべての収集は、最初はあらかじめ決められた量に達するまで中断されずに実行されます。
時間 (デフォルトでは 15 ミリ秒) が経過しました。これで収集が許可される場合
完全に完了するために、データ構造の変更を修正する必要がなくなります。
コレクション中。完了しない場合は、制御を
ミューテーターを作成し、後の処理中に少量の追加の GC 作業を実行します。
小さなオブジェクトの空きリストからは満たされない割り当て。いつ
マーキングが完了すると、変更されたページのセットが取得され、一度マークされます。
再びそれらのページ上のマークされたオブジェクトから、今度はミューテーターが停止されました。

私たちは、いくつかの異なるメカニズムの 1 つを使用して、変更されたページを追跡します。

* (`MPROTECT_VDB`) 物理ページを書き込み保護し、書き込みをキャッチすることによって
欠点。これは、多くの UNIX 系システムと Win32 に実装されています。それは
一部の環境では不可能です。
* (`GWW_VDB`) Win32`GetWriteWatch`関数を使用してダーティを読み取る
ビット。
* (`PROC_VDB`)`/proc`からダーティ ビット情報を取得します。 （現在
これをサポートしているのは Sun の Solaris だけです。これでかなりすっきりしましたが、
実際には、`mprotect`とシグナルを使用するとパフォーマンスが向上する可能性があります。)
* (`SOFT_VDB`)`/proc`から Linux のソフト ダーティ ビット情報を取得します。
* (`UFFDWP_VDB`) Linux`userfaultfd`サブシステムを使用することにより、
ライトプロテクトモード。
* 明示的なミューテーターの協力を通じて。これを可能にしたのは、
`GC_set_manual_vdb_allowed(1)`呼び出し。呼び出すにはクライアント コードが必要です。
`GC_ptr_store_and_dirty`または`GC_end_stubborn_change`(その後に数字が続きます)
`GC_reachable_here`呼び出しの)、めったに使用されません。
* (`DEFAULT_VDB`) すべてのページをダーティとして扱うことによって。これがデフォルトです
他のテクニックがどれも使用可能であることがわかっていない場合。 (実用のみ
テスト中。）

## ブラックリストへの登録

コレクターは、「」で説明されているように、ページの_ブラックリスト_を実装します。
[スペース効率の良い保守的なコレクション](https://dl.acm.org/doi/10.1145/173262.155109)
ベーム著、PLDI'93、も入手可能
[こちら](https://www.cs.rice.edu/~javaplt/311/Readings/pldi93.pdf)。

マークフェーズ中、コレクターはニアミス、つまり試みを追跡します。
ガベージ コレクションされたヒープのすぐ外への _ポインタ_ をたどる、または
ヒープ内の現在割り当てられていないページ。対象となったページ
このようなニアミスのうちの 1 つは、誤認された _pointers_ のターゲットになる可能性があります。
将来。このような誤認による今後の被害を最小限に抑えるため、
それらは、ポインタのない小さなオブジェクトにのみ割り当てられます。

コレクターは 2 つの異なる種類のブラックリストを理解しています。ページは次の場合があります
内部ポインター参照 (__INLINECODE 0__) のブラックリストに登録されています。
屋内が必要な場所からヒヤリハットの対象となった場合
ポインタ認識、例:スタック、または`GC_all_interior_pointers`の場合はヒープ
が設定されています。この場合、これを含む大きなブロックの割り当ても回避します。
ページ。

ニアミスが内部ポインタを必要としないソースからのものである場合
認識されると、`GC_add_to_black_list_normal`でブラックリストに登録されます。ページ
この方法でブラックリストに登録されている場合は、大きなオブジェクトの内部に表示される可能性があります。
大きなオブジェクトの最初のページではありません。

`GC_allochblk`ルーチンは、ブロックを割り当てるときにブラックリストを尊重します。
特定のオブジェクトの種類とサイズ。時々ドロップします（つまり、割り当てと
忘れた場合）過度の回避のために完全にブラックリストに登録されたブロック
使用できないブロックのみを含む長い大ブロックの空きリスト。これは
そうしないと、小さなポインタフリーの需要が低い場合に問題になります。
オブジェクト。

## マルチスレッドのサポート

いくつかのマルチスレッド モデルをサポートしています。残念ながら、pthreads では、
適度に標準化されたスレッドモデルのみで、サポート範囲が狭すぎます
保守的なガベージ コレクションのためのインターフェイス。ないようです
コレクターがさまざまなアイテムと共存できる完全にポータブルな方法
pthreads の実装。したがって、現在はより一般的なもののみをサポートしています
pthreads の実装。

特に、コレクターにとって他のすべてのサービスを停止することは非常に困難です。
システム内のスレッドを調べて、レジスタの内容を調べます。これは現在、
一部の pthread 実装では、非常に異なるメカニズムで実現されます。
Linux/HPUX/Tru64、Solaris、Irix の場合は、個々の pthread にシグナルを送信します。
そしてシグナルハンドラー内で待機させます。

Linux と Irix の実装では、文書化された pthreads 呼び出しのみが使用されますが、
rely on extensions to their semantics. Linuxの実装
`pthread_stop_world.c`ファイルは、非常に穏やかな拡張子のみに依存しています。
`pthreads`セマンティクスを備えており、他の UNIX に似た多数の機能をすでにサポートしています
`pthreads`の実装。私たちの目標は、これを唯一の`pthreads`にすることです。
コレクターでのサポート。

すべての実装は、スレッドの作成とその他のいくつかのプロセスをインターセプトする必要があります。
スレッド固有の呼び出しにより、スレッドとスレッドの位置を列挙できるようになります。
スタックします。これは現在、`gc.h`ファイル内の`#define`を使用して実現されています (実際には
`gc_pthread_redirects.h`ファイル)、またはオプションで`ld`の関数呼び出しを使用
Linux でのラッピング機構。

コレクターは、プロセッサーのスケーラビリティーを強化するためのいくつかの機能をサポートします。
コレクターのスレッドパフォーマンス。これらについてはさらに詳しく説明します
[ここ](scale.md)。スレッドローカルへのデータアプローチの概要を簡単に説明します。
割り当ては次のセクションで説明します。

## スレッドローカル割り当て

スレッドローカル割り当てが有効な場合 (デフォルトでは true)
サポートされているほとんどのプラットフォームの構成）、コレクターは個別に保持されます
各スレッドの空きリストの配列。

各スレッドに関連付けられたフリーリスト配列は、次の条件を満たすためにのみ使用されます。
非常に小さいオブジェクトと、小規模なオブジェクトのいずれかに属するオブジェクトのリクエスト
よく知られた種類の数々。これらには、_normal_、ポインターフリー、_gcj_、および
_disclaim_ オブジェクト。

スレッドローカルのフリーリストエントリには、最初の要素へのポインタが含まれます。
フリーリストの、または割り当て数のカウンターが含まれています
これまでに割り当てられたこのサイズのオブジェクトに対応する顆粒。最初は
これらには値 1、つまり小さなカウンタ値が含まれます。

スレッドローカル割り当ては、グローバル アロケータを介して直接行われます。
オブジェクトのサイズまたは種類は、ローカルの空きリストでカバーされていません。

適切なローカル フリー リストがある場合、アロケーターは次のことを確認します。
十分に小さなカウンター値が含まれています。そうであれば、カウンターは単純に
値が増加し、グローバル アロケータが使用されます。このようにして、
特定のサイズの最初のいくつかの割り当ては、ローカル アロケーターをバイパスします。
特定のサイズの少数のオブジェクトのみを割り当てるスレッドは、
そのサイズに対して独自の空きリストを構築します。これにより、スペースの無駄を回避できます。
人気のないオブジェクトのサイズや種類。

カウンタがしきい値を超えると、`GC_malloc_many`が呼び出されて割り当てられます。
およそ`HBLKSIZE`スペースを確保し、対応するローカルの空きリストに置きます。
そのサイズと種類のさらなる割り当てでは、この空きリストが使用され、
アロケータ ロックを取得する必要がなくなりました。割り当て手順
その他の点では、グローバル フリー リストと同様です。ローカルのフリーリストも
オブジェクト内の最初のポインターを使用してリンクされます。ほとんどの場合、これは、
必要な時間が大幅に短縮されます。

ローカルのフリー リストは、残りのコレクターのほとんどによって次のように扱われます。
それらは使用中の到達可能なデータでした。ポインタがないので、これには多少の注意が必要です
オブジェクトは通常はトレースされないため、特別なトレース手順が必要になります。
ポインタフリーリストおよび`gcj`ローカルフリーリスト上のすべてのオブジェクトをマークするために必要です。

スレッドの終了時に、残りのスレッドローカルの空きリスト エントリが転送されます。
グローバルフリーリストに戻ります。

コレクターがスレッドローカル割り当て用に構成されている場合 (
ほとんどのプラットフォームのデフォルト)、`GC_malloc`とその友人はスレッドローカルのみを使用します
割り当て。

詳細については、[こちら](scale.md) と次のタイトルの技術レポートを参照してください。
[高速マルチプロセッサ メモリ割り当てとガベージ コレクション](https://www.researchgate.net/publication/242553754_Fast_multiprocessor_memory_allocation_and_garbage_collection)。
